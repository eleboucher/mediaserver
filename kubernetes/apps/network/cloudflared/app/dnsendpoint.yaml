---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: cloudflared
spec:
  endpoints:
    - dnsName: external.erwanleboucher.dev
      recordType: CNAME
      targets:
        - ${TUNNEL_ID}.cfargotunnel.com
