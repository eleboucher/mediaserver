---
# yaml-language-server: $schema=https://k8s-schemas.erwanleboucher.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: towonel-agent
spec:
  chartRef:
    kind: OCIRepository
    name: towonel-agent
  interval: 1h
  values:
    controllers:
      towonel-agent:
        type: deployment
        replicas: 1
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          init-invite:
            image:
              repository: git.erwanleboucher.dev/eleboucher/towonel-agent
              tag: 0.0.1
            command:
              - /bin/sh
              - -c
              - |
                [ -f /root/.towonel/state.toml ] && echo "already initialized" && exit 0
                towonel-agent init --invite "$TOWONEL_INVITE_TOKEN"
            envFrom: &envFrom
              - secretRef:
                  name: towonel-agent-secret
            securityContext: &securityContext
              allowPrivilegeEscalation: false
        containers:
          app:
            image:
              repository: git.erwanleboucher.dev/eleboucher/towonel-agent
              tag: 0.0.1
            env:
              RUST_LOG: info
              TOWONEL_AGENT_SERVICES: '[{"hostname":"*.erwanleboucher.dev","origin":"envoy-external.network.svc.cluster.local:443"}]'
            probes:
              liveness:
                enabled: false
              readiness:
                enabled: false
            securityContext: *securityContext
    persistence:
      state:
        type: persistentVolumeClaim
        accessMode: ReadWriteOnce
        size: 100Mi
        globalMounts:
          - path: /root/.towonel
