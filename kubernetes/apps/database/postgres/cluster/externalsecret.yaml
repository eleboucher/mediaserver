---
# yaml-language-server: $schema=https://k8s-schemas.erwanleboucher.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: barman-ovh
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    template:
      metadata:
        labels:
          cnpg.io/reload: "true"
      data:
        OVH_ACCESS_KEY_ID: "{{ .AWS_ACCESS_KEY_ID }}"
        OVH_SECRET_ACCESS_KEY: "{{ .AWS_SECRET_ACCESS_KEY }}"
        S3_ENDPOINT: "{{ .S3_ENDPOINT }}"
  dataFrom:
    - extract:
        key: ovh-cnpg-backup
