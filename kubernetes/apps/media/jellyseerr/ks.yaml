# yaml-language-server: $schema=https://k8s-schemas.erwanleboucher.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
---
# yaml-language-server: $schema=https://k8s-schemas.erwanleboucher.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: jellyseerr
  namespace: flux-system
spec:
  interval: 1h
  components:
    - ../../../../components/volsync
    - ../../../../components/anubis
  path: ./kubernetes/apps/media/jellyseerr/app
  dependsOn:
    - name: envoy-gateway
      namespace: network
    - name: longhorn
      namespace: longhorn-system
  postBuild:
    substitute:
      APP: jellyseerr
      APP_NAMESPACE: media
      ANUBIS_TARGET: "http://jellyseerr.media.svc:5055"
      VOLSYNC_CAPACITY: 2Gi
      VOLSYNC_SCHEDULE: "10 1-23/2 * * *"
  prune: true
  sourceRef:
    kind: Bucket
    name: flux-system
    namespace: flux-system
  targetNamespace: media
  timeout: 5m
  wait: false
