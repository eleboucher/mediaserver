---
# yaml-language-server: $schema=https://k8s-schemas.erwanleboucher.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: bluesky-secret
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: bluesky-secret
    creationPolicy: Owner
    template:
      data:
        COCOON_ADMIN_PASSWORD: "{{ .ADMIN_PASSWORD }}"
        COCOON_SESSION_SECRET: "{{ .JWT_SECRET }}"
        COCOON_S3_ACCESS_KEY: "{{ .S3_ACCESS_KEY }}"
        COCOON_S3_SECRET_KEY: "{{ .S3_SECRET_KEY }}"
        COCOON_SMTP_HOST: "{{ .smtp_relay_server }}"
        COCOON_SMTP_PASS: "{{ .smtp_relay_password }}"
        COCOON_SMTP_USER: "{{ .smtp_relay_username }}"
  dataFrom:
    - extract:
        key: bluesky
    - extract:
        key: smtp-relay-secret
