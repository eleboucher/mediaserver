---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: snapshot-controller
  namespace: system
spec:
  chartRef:
    kind: OCIRepository
    name: snapshot-controller
  interval: 30m
  values:
    controller:
      replicaCount: 2
      serviceMonitor:
        create: true
