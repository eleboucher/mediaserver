---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: lldap-auth
  namespace: system
spec:
  targetRef:
    group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: lldap
  extAuth:
    http:
      backendRefs:
        - name: authelia
          namespace: system
          port: 9091
      path: /api/authz/forward-auth
      headersToBackend:
        - Remote-User
        - Remote-Groups
        - Remote-Email
        - Remote-Name
